{{#if icon}}
  {{{icon}}}
{{/if}}
<input
  class="{{#if icon}}input_icon{{/if}} {{class}}"
  {{#if placeholder}}
    placeholder="{{placeholder}}"
  {{/if}}
  {{#if type}}
    type="{{type}}"
  {{/if}}
  {{#if name}}
  name="{{name}}"
  {{/if}}
/>
