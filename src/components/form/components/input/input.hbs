{{#if labelText}}
  <label class="controls__label" for="{{name}}">{{labelText}}</label>
{{/if}}
<input
  name="{{name}}"
  placeholder="{{placeholder}}"
  required="{{isRequired}}"
  type="{{type}}"
  class="controls__input"
/>
